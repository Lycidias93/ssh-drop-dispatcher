# v4.13.0-verify-owner-rc2

Status: source candidate

RC2 keeps the verified RC1 delivery engine and adds a dedicated automation-first CLI/Termux layer.

## Main changes

- New primary `sdd` CLI with stable subcommands for status, targets, target tests, dispatch, delivery status/wait, requeue, logs and diagnostics.
- Machine-readable `--env` and `--json` status/capability/context output.
- `SDD_CHATGPT_CONTEXT_V1`: compact redacted context for ChatGPT/Codex without SSH hosts, remote paths, network addresses or secret content.
- Centralized Termux bridge v2 installs both `sdd` and legacy `dispatch-config`, rejects control-character arguments and avoids `eval`.
- Modernized doctor output with current explicit `shell=` profiles, config lint and a redacted ChatGPT mode.
- CLI version is sourced from installed `module.prop` instead of a hard-coded release string.

## Version

```text
version=4.13.0-verify-owner-rc2
versionCode=4130002
```

## Verification ownership remains unchanged

```text
verify_owner=dispatcher
external_verify_wrapper=no
remote_sha_required=yes
bash_missing_fallback=fail_closed
python_delivery=unsupported
policy=sdd-v4.13.0-verify-owner-v1
```

RC2 does not change the ownership policy version because the remote verification semantics are unchanged from RC1.

## CLI fixtures

Development fixture:

```text
RESULT: SDD_CLI_V2_CONTRACT_FIXTURES_PASS version=4.13.0-verify-owner-rc2 cli_schema=2 chatgpt_context_schema=1
```

The fixture checks shell syntax, version metadata, CLI schemas, noninteractive failure behavior, redaction, JSON/env output and generated Termux bridge syntax.

## Release boundary

This commit set prepares source RC2 only. `update.json` remains on the stable public release until an RC2 Magisk ZIP is built and installed-runtime verification succeeds.
